# Configuration

Configuration in this project is centered around:

- `env.example` (template values)
- `docker/.env` (local overrides, gitignored)
- `docker/docker-compose.yml` (service wiring)

For full variable details, see the `Environment Variables (docker/.env)` section in the root `README.md`.
